# Reference for the server environment file.
# setup-server.sh writes the real one to /etc/mission-deck.env with a generated
# DB password (root:www-data, mode 640). This file is only documentation.
DB_HOST=localhost
DB_NAME=missiondeck
DB_USER=missiondeck
DB_PASSWORD=generated-by-setup-server.sh
YII_ENV=prod
